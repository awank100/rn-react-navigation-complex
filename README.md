# rn-react-navigation-complex

Implement react-navigation in react native with complex / standar case.
1. Home Screen ( Bottom Tab)
1. When on `Profile Tab` and `isLogin === false`, it will redirect to Login Screen
1. `Topup Stack` inside `Profile Stack`